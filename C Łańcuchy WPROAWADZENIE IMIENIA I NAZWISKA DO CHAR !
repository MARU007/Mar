# Mar
C
#include <stdio.h>
#include <stdlib.h>
#define N 100

int main()
{   char z[N][N];
    int i=0,j;
    printf("Podaj imię i nazwisko:\n");
    do{

    scanf("%s", z[i]);
    i++;
    }
    while (getchar()!='\n');
    printf("Nazwywasz sie %s a na nazwisko masz %s", z[0], z[1]);

    return 0;
}
